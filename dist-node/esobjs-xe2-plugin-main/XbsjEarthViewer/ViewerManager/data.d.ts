declare type clound = {
    slice: number;
    brightness: number;
    maximumSize: [number, number, number];
    color: [number, number, number, number];
    scale: [number, number];
    position: [number, number, number];
}[];
export declare const cloudCollection: clound;
export {};
