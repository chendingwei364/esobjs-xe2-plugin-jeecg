import { UeObject, UeViewer } from '../../../base';
import { ESScale } from '../../objs';
export declare class UeESScale extends UeObject<ESScale> {
    static readonly type: void;
    constructor(sceneObject: ESScale, ueViewer: UeViewer);
}
