export declare type ESFlyToParam = {
    distance: number;
    heading: number;
    pitch: number;
    flyDuration: number;
    hDelta: number;
    pDelta: number;
};
export declare type ESFlyInParam = {
    position: [number, number, number];
    rotation: [number, number, number];
    flyDuration: number;
};
